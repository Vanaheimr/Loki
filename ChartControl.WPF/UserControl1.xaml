<UserControl x:Class="eu.Vanaheimr.Loki.ChartControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="450" SnapsToDevicePixels="True">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="*"  />
        </Grid.RowDefinitions>

        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="25" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Name="DeleteChartButton" Margin="4" FontWeight="Bold" FontSize="10" Click="DeleteChartButton_Click">X</Button>

        </Grid>

        <Canvas Grid.Row="1" Name="ChartCanvas1" />

    </Grid>

</UserControl>
